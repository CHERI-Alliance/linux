
struct foo * foos[10] = {
	a, b, c,
};

struct bar bars[] = {
};

struct baz bazs = {
};

int f(void)
{
}

int g(void);

struct data datas[] = {
	{
		.driver_data = (uintptr_t)foos,
		.driver_data = (uintptr_t)bars,
		.driver_info = (uintptr_t)others,
		.driver_info = (uintptr_t)f,
		.driver_data = (uintptr_t)f,
		.driver_data = (uintptr_t)g,
		.driver_data = (uintptr_t)g,
		.driver_data = (uintptr_t)bazs,
	},
	{
		.driver_data = (uintptr_t)0,
		.driver_data = (uintptr_t)0,
	},
	{
		.driver_data = (uintptr_t)foos,
		.driver_data = (uintptr_t)bars,
		.driver_data = (uintptr_t)others,
		.driver_data = (uintptr_t)f,
		.driver_info = (uintptr_t)f,
		.driver_info = (uintptr_t)g,
		.driver_data = (uintptr_t)g,
		.driver_data = (uintptr_t)bazs,
	},
	{
		.driver_info = (uintptr_t)foos,
		.driver_data = (uintptr_t)bars,
		.driver_data = (uintptr_t)others,
		.driver_info = (uintptr_t)f,
		.driver_data = (uintptr_t)f,
		.driver_data = (uintptr_t)g,
		.driver_data = (uintptr_t)g,
		.driver_data = (uintptr_t)bazs,
	},
	{
		.driver_info = (other)foos,
		.driver_data = (other)bars,
		.driver_data = (other)others,
		.driver_info = (other)f,
		.driver_data = (other)f,
		.driver_data = (other)g,
		.driver_data = (other)g,
		.driver_data = (other)bazs,
	},
};
