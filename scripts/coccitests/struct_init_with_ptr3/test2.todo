
struct foo * foos[10] = {
	a, b, c,
};

struct bar bars[] = {
};

struct baz bazs = {
};

int f(void)
{
}

int g(void);

struct data datas[] = {
	{
		.driver_info = (kernel_ulong_t)foos,
		.driver_info = (kernel_ulong_t)bars,
		.driver_info = (kernel_ulong_t)others,
		.driver_info = (kernel_ulong_t)f,
		.driver_info = (unsigned long)f,
		.driver_info = (kernel_ulong_t)g,
		.driver_info = (unsigned long)g,
		.driver_info = (kernel_ulong_t)bazs,
	},
	{
		.driver_info = (kernel_ulong_t)0,
		.driver_info = (unsigned long)0,
	},
	{
		.driver_info = (kernel_ulong_t)foos,
		.driver_info = (kernel_ulong_t)bars,
		.driver_info = (kernel_ulong_t)others,
		.driver_info = (kernel_ulong_t)f,
		.driver_info = (unsigned long)f,
		.driver_info = (kernel_ulong_t)g,
		.driver_info = (unsigned long)g,
		.driver_info = (kernel_ulong_t)bazs,
	},
	{
		.driver_info = (unsigned long)foos,
		.driver_info = (unsigned long)bars,
		.driver_info = (unsigned long)others,
		.driver_info = (kernel_ulong_t)f,
		.driver_info = (unsigned long)f,
		.driver_info = (kernel_ulong_t)g,
		.driver_info = (unsigned long)g,
		.driver_info = (unsigned long)bazs,
	},
	{
		.driver_info = (other)foos,
		.driver_info = (other)bars,
		.driver_info = (other)others,
		.driver_info = (other)f,
		.driver_info = (other)f,
		.driver_info = (other)g,
		.driver_info = (other)g,
		.driver_info = (other)bazs,
	},
};
