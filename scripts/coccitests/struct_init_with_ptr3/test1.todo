
struct foo * foos[10] = {
	a, b, c,
};

struct bar bars[] = {
};

struct baz bazs = {
};

int f(void)
{
}

int g(void);

struct data datas[] = {
	{
		.driver_data = (kernel_ulong_t)foos,
		.driver_data = (kernel_ulong_t)bars,
		.driver_data = (kernel_ulong_t)others,
		.driver_data = (kernel_ulong_t)f,
		.driver_data = (unsigned long)f,
		.driver_data = (kernel_ulong_t)g,
		.driver_data = (unsigned long)g,
		.driver_data = (kernel_ulong_t)bazs,
	},
	{
		.driver_data = (kernel_ulong_t)0,
		.driver_data = (unsigned long)0,
	},
	{
		.driver_data = (kernel_ulong_t)foos,
		.driver_data = (kernel_ulong_t)bars,
		.driver_data = (kernel_ulong_t)others,
		.driver_data = (kernel_ulong_t)f,
		.driver_data = (unsigned long)f,
		.driver_data = (kernel_ulong_t)g,
		.driver_data = (unsigned long)g,
		.driver_data = (kernel_ulong_t)bazs,
	},
	{
		.driver_data = (unsigned long)foos,
		.driver_data = (unsigned long)bars,
		.driver_data = (unsigned long)others,
		.driver_data = (kernel_ulong_t)f,
		.driver_data = (unsigned long)f,
		.driver_data = (kernel_ulong_t)g,
		.driver_data = (unsigned long)g,
		.driver_data = (unsigned long)bazs,
	},
	{
		.driver_data = (other)foos,
		.driver_data = (other)bars,
		.driver_data = (other)others,
		.driver_data = (other)f,
		.driver_data = (other)f,
		.driver_data = (other)g,
		.driver_data = (other)g,
		.driver_data = (other)bazs,
	},
};
