
struct foo * foos[10] = {
	a, b, c,
};

struct bar bars[] = {
};

struct baz bazs = {
};

int f(void)
{
}

int g(void);

struct data datas[] = {
	{
		(kernel_ulong_t)foos,
		(kernel_ulong_t)bars,
		(kernel_ulong_t)others,
		(kernel_ulong_t)f,
		(unsigned long)f,
		(kernel_ulong_t)g,
		(unsigned long)g,
		(kernel_ulong_t)bazs,
	},
	{
		(kernel_ulong_t)0,
		(unsigned long)0,
		(unsigned long)(7+8),
	},
	{
		(kernel_ulong_t)foos,
		(kernel_ulong_t)bars,
		(kernel_ulong_t)others,
		(kernel_ulong_t)f,
		(unsigned long)f,
		(kernel_ulong_t)g,
		(unsigned long)g,
		(kernel_ulong_t)bazs,
	},
	{
		(unsigned long)foos,
		(unsigned long)bars,
		(unsigned long)others,
		(kernel_ulong_t)f,
		(unsigned long)f,
		(kernel_ulong_t)g,
		(unsigned long)g,
		(unsigned long)bazs,
	},
	{
		(other)foos,
		(other)bars,
		(other)others,
		(other)f,
		(other)f,
		(other)g,
		(other)g,
		(other)bazs,
	},
};
